#include"main.h"
/**
 * _islower - main function
 *
 * @c: character to compare
 *
 * Return: 1 if true, 0 if false
 *
 */

int main(void)
{
 int c = 'c';
	if (c >= 'a' && c <= 'z')
	{
		printf ("%d",1);
	}

	else{
	printf ("%d", 0);
	}
	
	return (0);
}

